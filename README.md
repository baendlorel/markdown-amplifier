# Markdown Amplifier

此为个人知识库

.marc.json 中配置了加密要忽略的文件夹，可以手动修改。

```json
{
  "locale": "zh",
  "crypt": {
    "encryptFileName": true,
    "encryptFolderName": true,
    "exclude": [],
    "dir": {
      "decrypted": "decrypted",
      "encrypted": "encrypted"
    }
  },
  "number": {}
}
```
